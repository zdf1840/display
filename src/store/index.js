import Vue from 'vue'
import Vuex from 'vuex'
import dataV from '@jiaminghi/data-view'

Vue.use(dataV)

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
